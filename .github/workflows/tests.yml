# .github/workflows/tests.yml
name: Tests

on:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  RAILS_ENV: test
  NODE_TLS_REJECT_UNAUTHORIZED: 0
  NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
  STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
  WISE_API_KEY: ${{ secrets.WISE_API_KEY }}
  WISE_PROFILE_ID: ${{ secrets.WISE_PROFILE_ID }}

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      # Enhanced caching strategy
      - name: Cache Ruby gems
        uses: actions/cache@v4
        with:
          path: backend/vendor/bundle
          key: gems-${{ runner.os }}-${{ hashFiles('backend/Gemfile.lock') }}
          restore-keys: gems-${{ runner.os }}-

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: pnpm-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Cache Next build
        id: next-cache
        uses: actions/cache@v4
        with:
          path: |
            frontend/.next/cache
            frontend/node_modules/.cache
            frontend/.next
          key: next-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('frontend/**/*.{ts,tsx,js,jsx}') }}
          restore-keys: |
            next-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}-
            next-${{ runner.os }}-

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: playwright-${{ runner.os }}-

      - uses: ./.github/common/

      - name: Install all dependencies and tools
        run: |
          set -e  # Exit on any error
          REPO_ROOT=$(pwd)

          # Install system dependencies first (can't be parallelized)
          sudo apt update
          sudo apt install libnss3-tools

          # Download mkcert binary
          curl -JLO "https://dl.filippo.io/mkcert/latest?for=linux/amd64" &
          MKCERT_DOWNLOAD_PID=$!

          # Configure bundler to use cache
          cd "$REPO_ROOT/backend"
          bundle config path vendor/bundle
          bundle install --jobs 4 &
          BUNDLE_PID=$!

          # Install pnpm deps from root
          cd "$REPO_ROOT"
          pnpm install &
          PNPM_PID=$!

          # Install Playwright browsers if not cached
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" != "true" ]; then
            pnpm playwright install --with-deps chromium &
            PLAYWRIGHT_PID=$!
          fi

          # Install foreman
          gem install foreman &
          GEM_PID=$!

          # Wait for mkcert download and install it
          wait $MKCERT_DOWNLOAD_PID || { echo "mkcert download failed"; exit 1; }
          chmod +x mkcert-v*-linux-amd64
          sudo cp mkcert-v*-linux-amd64 /usr/local/bin/mkcert &
          MKCERT_INSTALL_PID=$!

          # Wait for all installations and check exit codes
          wait $BUNDLE_PID || { echo "Bundle install failed"; exit 1; }
          wait $PNPM_PID || { echo "pnpm install failed"; exit 1; }
          wait $GEM_PID || { echo "Gem install failed"; exit 1; }
          wait $MKCERT_INSTALL_PID || { echo "mkcert install failed"; exit 1; }

          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" != "true" ]; then
            wait $PLAYWRIGHT_PID || { echo "Playwright install failed"; exit 1; }
          fi

          echo "All dependencies and tools installed successfully!"

      - name: Create certificates
        run: |
          mkdir -p certificates
          node docker/createCertificate.js

      - name: Build Next.js
        run: |
          if [ ! -d "frontend/.next" ] || [ "${{ steps.next-cache.outputs.cache-hit }}" != "true" ]; then
            echo "Building Next.js..."
            NODE_ENV=test pnpm run build-next --no-lint
          else
            echo "Using cached Next.js build"
          fi

      # Cache the complete setup for test jobs
      - name: Cache complete setup
        uses: actions/cache@v4
        with:
          path: |
            backend/vendor/bundle
            node_modules
            frontend/node_modules
            frontend/.next
            ~/.cache/ms-playwright
            certificates
          key: setup-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock', '**/pnpm-lock.yaml') }}-${{ github.sha }}

  playwright-tests:
    name: playwright-${{ matrix.shardIndex }}
    runs-on: ubuntu-latest
    needs: setup
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        shardIndex: [1, 2, 3, 4]
        shardTotal: [4]

    services:
      postgres:
        image: postgres:16.3
        env:
          POSTGRES_USER: username
          POSTGRES_PASSWORD: password
          POSTGRES_DB: flexile_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7.4.2
        ports:
          - 6389:6379
        options: --entrypoint redis-server

    steps:
      - uses: actions/checkout@v4

      # Restore complete setup from cache with fallback
      - name: Restore complete setup
        id: restore-setup
        uses: actions/cache@v4
        with:
          path: |
            backend/vendor/bundle
            node_modules
            frontend/node_modules
            frontend/.next
            ~/.cache/ms-playwright
            certificates
          key: setup-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock', '**/pnpm-lock.yaml') }}-${{ github.sha }}
          restore-keys: |
            setup-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock', '**/pnpm-lock.yaml') }}-
            setup-${{ runner.os }}-

      # Fallback setup if cache miss (shouldn't happen with needs: setup, but safety net)
      - name: Fallback setup (if cache miss)
        if: steps.restore-setup.outputs.cache-hit != 'true'
        run: |
          echo "⚠️ Cache miss detected - running fallback setup"
          echo "This shouldn't happen with needs: setup, investigating..."

          # Quick dependency install as fallback
          sudo apt update && sudo apt install libnss3-tools

          # Install bundler dependencies
          cd backend
          bundle config path vendor/bundle
          bundle install --jobs 4

          # Install pnpm dependencies
          cd ..
          pnpm install

          # Install playwright browsers
          pnpm playwright install --with-deps chromium

          # Create certificates
          mkdir -p certificates
          node docker/createCertificate.js

          # Build Next.js
          NODE_ENV=test pnpm run build-next --no-lint

          echo "✅ Fallback setup completed"

      - uses: ./.github/common/

      - name: Trust self-signed certificates
        run: |
          # Ensure mkcert is available (lightweight install)
          sudo apt update && sudo apt install libnss3-tools
          if [ -f "certificates/rootCA.pem" ]; then
            sudo cp certificates/rootCA.pem /usr/local/share/ca-certificates/mkcert-rootCA.crt
            sudo update-ca-certificates
          fi

      - name: Setup test database
        run: |
          cd backend
          bundle config path vendor/bundle
          bundle exec rails db:prepare

      - name: Start all services
        run: |
          cd backend

          # Start Rails server
          bundle exec rails server -p 3100 -e test -b 0.0.0.0 &
          RAILS_PID=$!

          # Start Sidekiq
          bundle exec sidekiq -q default -q mailers &
          SIDEKIQ_PID=$!

          # Start Next.js
          cd ..
          TZ=UTC [ -n "$CI" ] && node e2e/next.js &
          NEXT_PID=$!

          # Start Inngest
          pnpm inngest-cli dev --no-discovery -p 8298 -u https://test.flexile.dev:3101/api/inngest &
          INNGEST_PID=$!

          # Parallel health checks with faster polling
          echo "Waiting for services to start..."
          timeout 30 bash -c 'until curl -s http://localhost:3100/health > /dev/null 2>&1; do sleep 0.5; done' &
          RAILS_HEALTH=$!

          timeout 30 bash -c 'until curl -k https://test.flexile.dev:3101/api/health > /dev/null 2>&1; do sleep 0.5; done' &
          NEXT_HEALTH=$!

          # Wait for health checks to complete
          wait $RAILS_HEALTH $NEXT_HEALTH
          echo "All services are ready!"

      - name: Run Playwright tests
        run: |
          pnpm playwright test \
            --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }} \
            --workers=2 \
            --max-failures=5
        timeout-minutes: 10

      - name: Upload blob report to GitHub Actions Artifacts
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: blob-report-${{ matrix.shardIndex }}
          path: blob-report
          retention-days: 1

  merge-reports:
    if: ${{ !cancelled() }}
    needs: [playwright-tests]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Load test environment
        run: |
          if [ -f .env.test ]; then
            echo "Loading environment from .env.test"
            cat .env.test >> $GITHUB_ENV
          fi

      - uses: ./.github/common/

      - name: Download blob reports from GitHub Actions Artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-blob-reports
          pattern: blob-report-*
          merge-multiple: true

      - name: Merge into HTML Report
        run: pnpm playwright merge-reports --reporter html ./all-blob-reports

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: html-report--attempt-${{ github.run_attempt }}
          path: playwright-report
          retention-days: 14
